# Contexte actif

## État actuel

Application stabilisée sur React 19 avec React Compiler activé.

## Journal (extraits)

- **2026-06-07 (Activités — enfants participants, modale dédiée)** : **`ActiviteDto.enfants`** (**`EnfantParticipantInfo`**) ; **`CreateActiviteRequest` / `UpdateActiviteRequest.enfantIds`** (liste complète, `[]` = aucun). **`ActiviteEnfantsParticipantsModal.tsx`** (A) : ouverture → **`GET …/activites/{id}`** ; liste **Participants actuels** ; bouton **Modifier la sélection** → cases à cocher (enfants des **`groupeIds`** via **`enfantsEligiblesPourGroupesActivite`**) + recherche inline à côté du libellé **Sélection** ; conflits créneau **`idsEnfantsDejaAffectesAutreActivite`** (hiérarchie moments) → nom grisé, **« Déjà sur « … » (moment) »** en ligne, case désactivée, **`aria-disabled`** sur label (évite curseur doigt global **`styles.scss`**). Enregistrement : **`activiteVersUpdateRequest`** + **`modifierActivite`**. **`ListeActivites.tsx`** : état modale, **`peutModifierActivite`** ; modale CRUD activité conserve **`enfantIds`** existants (pas d’édition enfants dans ce formulaire) ; création **`enfantIds: []`**. Accès : **`ListeActivitesListe`** (icône **`FaChild`** en-tête carte) + **`ListeActivitesCalendrier`** → **`CalendrierCarteEditionAvecSuppression.onEnfantsClick`** (barre latérale icônes, **`faChild`**). **Working tree** (non commité) : **`ActiviteEnfantsParticipantsModal`**, **`ListeActivites`**, **`ListeActivitesListe`**, **`ListeActivitesCalendrier`**, **`listeActivitesUtils`**, **`ListeActivites.module.scss`**, **`CalendrierCarteEditionAvecSuppression`**, **`PlanningCalendrier.module.scss`**, **`api.d.ts`**.

- **2026-06-07 (Chambres — impression liste filtrée)** : **`ListeChambres`** — **`PrintTrigger`** aligné à droite (**`.printTriggerRight`**) ; **`usePrintContent`** + **`PrintContentRoot`** + **`ListePrintTable`** (pattern **`ListeCahierInfirmerie`**, pas **`Liste`**) ; lignes = **`chambresFiltrées`** (type, genre, groupes, recherche identifiant/bâtiment/étage/couloir/occupant) ; titre **`Chambres du séjour`** ; métadonnées **`PrintDocumentHeader`** (résultats affichés + résumé filtres actifs). Colonnes print : **Chambre** (identifiant seul), **Genre** (**`GenreChambrePrintLabels`** : Filles / Garçons / Mixte), **Groupe**, **Localisation**, **Occupants**, **Référents** — localisation / occupants / référents sur **lignes séparées** (`\n` + **`white-space: pre-line`**) ; en-têtes centrés ; largeurs CSS injectées (**`CHAMBRES_PRINT_EXTRA_STYLE`**, classes **`enjoy-chambres-print-col-*`**). **`GenreChambre.ts`** : export **`GenreChambrePrintLabels`**. **Index stagé** : **`ListeChambres`**, **`ListeChambres.module.scss`**, **`GenreChambre.ts`**.

- **2026-06-07 (Activités — pré-validation conflits animateur / lieu en modale)** : **`listeActivitesUtils.ts`** — **`verifierDisponibiliteLieuActivite`** (chevauchement hiérarchique via **`idsEnConflit`**, aligné **`ActiviteServiceImpl.verifierDisponibiliteLieuPourActivite`**) : lieu non partageable → erreur bloquante ; partageable → plafond **`nombreMaxActivitesSimultanees`** ou **avertissement** si sous la limite ; messages incluent **« Occupé par : … »** (animateurs + activité + créneau réel des activités en conflit). **`premierConflitAnimateurActiviteInterne`** : blocage avant submit si un animateur coché a déjà une activité le même jour sur un moment en conflit hiérarchique (message avec chevauchement explicite). **`ListeActivites.tsx`** : contrôles dans **`handleSubmit`** ; aperçu live sous le champ Lieu (**`disponibiliteLieuFormulaire`**, **`useMemo`**) ; classes **`.lieuHintErreur`** / **`.lieuHintAvertissement`** (**`ListeActivites.module.scss`**). Retrait sortie calendrier : recherche activité interne avec **`idsEnConflit(moment.id, momentsTriés)`**. **`construireArbreMoments.ts`** : **`idsEnConflit`**, **`idsSousArbreMoment`**, **`ancetresMomentIds`** acceptent **`readonly MomentDto[]`**. Secours API : **`ANIMATEUR_DEJA_AFFECTE_CRENEAU`** (400) toujours via **`adaptAxiosError`**. **Index stagé** : **`ListeActivites`**, **`ListeActivites.module.scss`**, **`listeActivitesUtils`**, **`construireArbreMoments`**.

- **2026-06-07 (Moments — hiérarchie parent/enfants + conflits animateurs + accordéons)** : **Hiérarchie moments** — `MomentDto` enrichi : **`parentId: number | null`** + **`ordre: number`** ; `SaveMomentRequest` : **`parentId?: number | null`**. Nouveau helper **`construireArbreMoments.ts`** : `construireArbreMoments` (arbre `MomentArbreNode`), `aplatirMomentsHierarchiquement` (`MomentAplat` avec `profondeur`), `libelleMomentIndenté`, `idsSousArbreMoment`, `ancetresMomentIds`, **`idsEnConflit`** (moment + ancêtres + descendants — base règle conflit animateur), `parentsEligiblesPourMoment`, `enfantsEligiblesPourMoment`, `optionsEnfantsAffichables`, `idsRacinesSelectionEnfants`, `validerLiensParentEnfants`, `idsApresDeplacementMomentFrere`. **`trierMomentsChronologiquement`** : si `parentId` présent → `aplatirMomentsHierarchiquement` ; sinon tri `ordre` / `id`. **`ListeMoments.tsx`** : modale unifiée (parent select-unique + enfants multi-select checkboxes ; section « Enfants rattachés » si modification ; détection cycles `validerLiensParentEnfants`) ; affichage hiérarchique (`blocParent` / `blocEnfants` indenté + bordure gauche bleue) ; réordonnancement entre frères (`idsApresDeplacementMomentFrere`) ; erreur suppression dans la modale (`deleteErrorMessage`) ; **accordéons sous-moments** (`accordionsFermes: Set<number>`, bouton ▼/▶ sur cartes parents, ouvert par défaut) ; cartes compactes une ligne (nom + badge + ↑↓ + Modifier + Supprimer). **`ListeActivites`**, **`ListeActivitesPrestataires`**, **`ListePlanningsOrganisation`** : sélecteurs moments indentés via `aplatirMomentsHierarchiquement` + `libelleMomentIndenté`. **Conflits animateurs** — règle backend : un animateur ne peut pas avoir deux activités le même jour si leurs moments se chevauchent dans la hiérarchie (M ∪ ancêtres(M) ∪ descendants(M)) ; erreur `ANIMATEUR_DEJA_AFFECTE_CRENEAU` HTTP 400, `message` affiché tel quel (déjà extrait par `adaptAxiosError`). **`listeActivitesPrestatairesCalendrier.ts`** : `activiteInternePourTokenMomentDate` + `sortieVisiblePourTokenMomentDate` : param optionnel `conflictMomentIds?: Set<number>` ; `listerConflitsActiviteInterneAvecSortie` + `listerConflitsCalendrierPrestataire` calculent `idsEnConflit(momentId, moments)` et passent le set. **Index stagé** : `ListeActivites`, `ListeActivitesPrestataires`, `ListePlanningsOrganisation`, `ListeMoments` (+ SCSS), `listeActivitesPrestatairesCalendrier`, `construireArbreMoments` (A), `trierMomentsChronologiquement`, `api.d.ts`.

- **2026-06-07 (Impression — menus couleur/N&B + métadonnées allégées)** : **`DetailsSejourMenus`** — **`PrintTrigger`** ouvre modale **Noir et blanc** / **Couleurs** (styles **`printChoiceOption*`** dans **`DetailsSejourMenus.module.scss`**) ; mode **Couleurs** : fond cellules via **`couleurFondCarteMenuPourTypeRepas`** + classes **`menusPrintCellColor`** / **`menusPrintRepasBadgeColor`** (**`--enjoy-menus-print-cell-bg`**) ; preset **`menusGrid`** : libellés lignes repas centrés verticalement (**`menusPrintThRepas`**, vue liste date/badge). **`PrintDocumentHeader`** menus : titre seul (plus de métadonnées période/vue/mode). **Allègement métadonnées print** (choix déjà faits à l’écran) : **`ListeActivites`** — retiré période/vue/rendu (filtres animateurs/groupes conservés) ; **`ListePlanningsOrganisation`** — retiré période/vue (consigne globale conservée) ; **`ListeSanitaireDossiers`** — retiré colonnes optionnelles (effectif séjour conservé). **Index stagé** : **`DetailsSejourMenus`**, SCSS menus, **`ListeActivites`**, **`ListePlanningsOrganisation`**, **`ListeSanitaireDossiers`**, **`printGlobalClasses`**, **`printPageStyles`**.

- **2026-06-07 (Auth — redirect 401 global intercepteur Axios)** : **`caller.service.ts`** — sur **401** non récupérable (échec **`refreshAccessToken`** ou requête avec **`X-Skip-Token-Refresh`**) : mémorisation URL (**`enregistrerCheminApresConnexionDepuisNavigateur`**), **`accountService.logout()`**, **`window.location.replace('/')`** si pathname ≠ **`/`** ; garde-fou **`sessionExpiredRedirectPending`** (évite redirects multiples). **Exception** : échec login sur **`/`** (**`X-Skip-Token-Refresh`** + pathname **`/`**) → message d’erreur conservé, pas de redirect forcé. **`account.service.ts`** : **`refreshAccessToken`** envoie **`X-Skip-Token-Refresh`** sur **`POST /auth/refresh-token`** (évite boucle intercepteur). Complète **`routeError.ts`** / **`Layout`** (loaders et profil). **Index stagé** : **`caller.service.ts`**, **`account.service.ts`**.

- **2026-06-06 (Activités — impression vue calendrier)** : **`ListeActivites`** (vue **Calendrier** uniquement) — bouton **Imprimer** dans la barre outils ; modale choix **Noir et blanc** / **Couleurs** (styles **`printChoiceOption*`** ; description **`currentColor`** au survol). Rendu print dédié (**`renderPlanningPrintCalendrier`**) : grille animateurs × jours filtrée (**`equipePourCalendrier`**, **`joursFenetreCalendrier`**, filtres animateurs/groupes) ; sans actions édition/suppression ; activités, sorties prestataires, conflits. **Noir et blanc** : cartes fond blanc ; **Couleurs** : **`couleurFondCalendrierPourTypeActivite(typeActivite.id)`** (activités), **`rgba(13,110,92,0.14)`** (sorties). **`usePrintContent`** + **`PrintContentRoot`** + **`PrintDocumentHeader`** ; titre **`Planning d'activités — {nom séjour}`** ; **`format: 'landscape-a4'`** ; preset **`PRINT_STYLE_PRESETS.activitesCalendarGrid`** ; classes **`PRINT_GLOBAL_CLASS.activitesPrint*`** (**`printGlobalClasses.ts`**). **Index stagé** : **`ListeActivites`**, **`ListeActivites.module.scss`**, **`printPageStyles`**, **`printGlobalClasses`**.

- **2026-06-06 (Dossier enfant — impression fiche individuelle)** : route **`/mes-sejours/:id/enfants/:enfantId/dossier`** — **`DossierEnfant`** : bouton **Imprimer** (**`PrintTrigger`**) ; contenu des **4 sections** dans **`PrintContentRoot`** + **`usePrintContent`** ; **`PrintDocumentHeader`** (métadonnée **Séjour** : nom + dates) ; masquage print (**`PRINT_GLOBAL_CLASS.noPrint`**) : Retour, titre écran, crayons édition. En-tête répété dédié : option **`runningHeaderLayout: 'title-page-split'`** (**titre à gauche**, **n/N à droite**, marge haute **3mm** / **`@page` 16mm**) — **uniquement** ce consommateur ; défaut inchangé (**`Titre — n/N`** en **`@top-left`**) pour listes, cahier, plannings, etc. **`printPageStyles.ts`** : **`RunningHeaderLayout`**, paramètre **`buildPrintPageStyle`** / **`UsePrintContentOptions`**. **`DossierEnfant.module.scss`** : **`@media print`** (sections sans ombre, **`break-inside: avoid`**). **Index stagé** : **`DossierEnfant`**, **`DossierEnfant.module.scss`**, **`printPageStyles`**, **`types`**, **`usePrintContent`**.

- **2026-06-06 (Sanitaire — impression dossiers sanitaires + styles print listes)** : **`ListeSanitaireDossiers`** — **`canPrint`** via **`Liste`** + **`ListePrintActions`** (même pattern enfants/équipe) ; colonnes dynamiques (**cases barre** + filtres tableau) et lignes filtrées (**`ligneGardeePourGroupesColonnes`**, filtres **`Liste`**) ; **`ColumnConfig.printValue`** sur toutes les colonnes ; **`printHeaderContext`** (colonnes optionnelles actives, effectif séjour, filtres via **`libelleFiltresListeActifs`**) ; **`tableTopMargin`**. Colonnes : **Prénom** **`printNoWrap`** + **`min-width: 7rem`** ; **Groupe(s)** classe globale **`enjoy-sanitaire-col-groupes`** (~5,5–6 rem, écran + print preset **`listeTable`**) ; e-mails/tél. parents **`printNoWrap`**. **`ListeSanitaireDossiers.module.scss`** (A). **`printPageStyles.ts`** : **`vertical-align: middle`** sur **`th`/`td`** tableau liste print (en-têtes + données) ; règle print **`.enjoy-sanitaire-col-groupes`**. **Index stagé** : **`ListeSanitaireDossiers`**, **`ListeSanitaireDossiers.module.scss`**, **`printPageStyles`**.

- **2026-06-06 (Sanitaire — cahier d'infirmerie : colonnes & UX liste)** : **`ListeCahierInfirmerie`** — colonne **Localisation** (`localisationCorps`) écran + print ; libellé **Soigné(e) par** (remplace **Soigneur**) en en-tête, print et **`CahierInfirmerieForm`** ; colonne **Soigné(e) par** élargie (`min-width: 11rem` écran, classe print **`enjoy-cahier-print-col-soigne-par`**) ; colonne **Date** / **heure** sur **2 lignes** (en-tête + cellules + print `white-space: pre-line`, helpers locaux **`formaterDateHeureListeCahier`**) ; filtre texte insensible aux **accents** via **`normaliserPourRechercheTexte`** (`libelleChambre.ts`). **Index stagé** : **`ListeCahierInfirmerie`**, **`CahierInfirmerieForm`**.

- **2026-06-06 (Sanitaire — impression cahier d'infirmerie)** : **`ListeCahierInfirmerie`** — bouton **Imprimer** (**`PrintTrigger`**) à côté **Nouvelle entrée** ; barre outils **`lg="3"`** par colonne (évite chevauchement boutons). Impression via **`usePrintContent`** + **`PrintContentRoot`** + **`ListePrintTable`** (colonnes **`ColumnConfig.printValue`**, sans colonne **Actions** ni **Auteur**) ; colonne print dédiée **Signature de la direction** (cellules vides, bordures identiques aux autres colonnes — override preset **`listeTable`** `td:empty`) ; alignement centré en-têtes + cellules **Date** / **heure**, **Enfant**, **Appels**, **Soigné(e) par** (classe **`enjoy-cahier-print-cell-center`** via **`ColumnConfig.className`**, propagée par **`ListePrintTable`**). Titre document / en-tête répété : **`Cahier d'infirmerie — {nom séjour} ({date début} – {date fin})`** ; métadonnées **`PrintDocumentHeader`** (résultats filtrés, jour, recherche). Filtres jour + texte respectés (**`lignesFiltrees`**). **`ListePrintTable`** : applique aussi **`column.className`** sur **`th`/`td`** print. **Index stagé** : **`ListeCahierInfirmerie`**, **`ListePrintTable`**.

- **2026-06-06 (Listes — filtre multi-sélection en-tête + largeurs colonnes)** : nouveau helper **`helpers/multiselectFilter.ts`** — **`parseMultiselectFilterValue`** (string JSON `string[]` → `Set<string>`, valeur legacy ignorée), **`stringifyMultiselectFilterValue`** (Set → JSON, **`""`** si vide), **`libelleResumeMultiselectFilter`** (≤ 2 labels listés, sinon **« N sélectionné·s »**, **« Tous »** si vide). Composant **`CheckboxFilterDropdown.tsx`** (+ `.module.scss`) : bouton (résumé + chevron) ouvrant un panneau **`role="listbox"`** de cases reactstrap, **Tout désélectionner**, fermeture au clic extérieur (**`useRef`** + **`useEffect`** listener `mousedown`), **`useMemo`** pour le résumé, variante **`compact`** (filtres d’en-tête), prop **`panelExtra`** (contenu additionnel ex. groupes par type). **`Liste.tsx`** : **`ColumnConfig.filterType`** étend **`'multiselect'`** ; **`applyFilters`** — multiselect = **OU** sur valeurs cochées (`Set.has(String(itemValue))`), **vide ⇒ toutes les lignes** ; rendu du dropdown dans la rangée filtres ; **pas** de bouton « effacer » pour `select` **ni** `multiselect` ; `placeholder` texte = **`column.filterPlaceholder ?? column.label`** ; nouvelle prop **`tableClassName`** (classe sur le tableau écran). **`libelleFiltresListeActifs.ts`** (impression) : résumé multiselect via **`libelleResumeMultiselectFilter`**. **`ListeEnfants`** : colonne **Niveau scolaire** en **`filterType: 'multiselect'`** (`niveauScolaireFilterOptions` depuis **`NiveauScolaire`** / **`NiveauScolaireLabels`**) ; **Date de naissance** **`filterable: false`** ; classes largeurs colonnes (`colNom`, `colPrenom`, `colGenre`, `colNiveauScolaire`, `colDateNaissance`, `colGroupes`, `colChambre`) + **`tableClassName={styles.tableListeEnfants}`**. **`TableauUtilisateurs`** : **`filterPlaceholder: ""`** sur Chambre, **`tableClassName`** équipe (`tableEquipeMembres`). **SCSS** : `table-layout: auto` écran — petites colonnes (genre/niveau/date/âge/rôle/chambre/actions) à largeur **figée**, colonnes flexibles (nom/prénom/groupes/email) en **`min-width`** ; **email/téléphone** `white-space: nowrap` ; `colAge` 5.5→4.5rem, `actions_cell` largeur fixe 6.5rem. **Index stagé** : `CheckboxFilterDropdown` (A) + SCSS, `Liste` (+SCSS), `ListeEnfants` (+SCSS), `TableauUtilisateurs` (+SCSS), `multiselectFilter` (A), `libelleFiltresListeActifs`.

- **2026-06-06 (Impression listes — largeur colonnes, marges, URL)** : extraction du tableau print dans **`ListePrintTable.tsx`** (rendu **hors** `PrintContentRoot` côté écran — seuls en-tête doc + tableau print clonés par `react-to-print`). **`usePrintContent`** : option **`ignoreGlobalStyles`** (n’injecte que `pageStyle`, évite styles écran/Bootstrap clonés) — activée pour les listes ; `pageStyle` mémoïsé. **`printPageStyles.ts`** : marge bas de page **`PRINT_PAGE_MARGIN_BOTTOM = 20mm`** via **`@page margin`** (pas via padding — sinon marge seulement en fin de doc) ; **URL/pied navigateur masqués** par **`buildBrowserMarginBoxReset`** (toutes les margin boxes `@top-*`/`@bottom-*` à `content: ''`, sauf titre `@top-left`) — l’option « En-têtes et pieds » du navigateur reste hors contrôle CSS. Tableau liste print : **`display: table !important`** (forcé contre `.enjoy-print-only { display: block }` qui cassait `table-layout`), **`table-layout: auto`** + `width: 100%` → table pleine page, colonnes dimensionnées au contenu (Rôle compact, etc.). **`ColumnConfig.printNoWrap`** + classe **`PRINT_GLOBAL_CLASS.listePrintNoWrap`** (`white-space: nowrap`) : **Email** / **Téléphone** sur une seule ligne (**`TableauUtilisateurs`**). Helper **`buildListePrintExtraStyle`** réduit au preset **`listeTable`**. **Note React 19** : balises `<style>` hissées hors du DOM cloné → ne pas compter dessus pour le print (largeurs via `pageStyle` + inline/classes). **Index stagé** : `Liste`, `ListeCustomPrintSelection`, `ListePlanningsOrganisation`, `ListePrintTable` (A), `TableauUtilisateurs`, `print/index`, `listePrintTableLayout` (A), `printGlobalClasses`, `printPageStyles`, `types`, `usePrintContent`.

- **2026-06-06 (Impression plannings organisation + titre en-tête global)** : **`ListePlanningsOrganisation`** — bouton **Imprimer** (éditeur pleine page **`…/organisation/:grilleId`** ou modale tableau) via **`usePrintContent`** + **`PrintTrigger`** ; grille print dédiée (**`PRINT_GLOBAL_CLASS.only`**, **`renderPlanningPrintGrid`**) : colonnes = **`joursFenetre`** (période + vue **1 / 3 / 7 jours** affichées à l’écran, pas tout le séjour) ; sans actions / historique / édition ; **`format: 'landscape-a4'`**, presets **`wideTable`** + **`planningGrid`** ; métadonnées **`PrintDocumentHeader`** (période, vue, consigne). **`printPageStyles.ts`** — en-tête répété **Titre — n/N** : **13pt**, **gras 700** (tous consommateurs **`src/print/`**) ; **`PrintDocumentHeader`** : métadonnées seules (titre **non** dupliqué sous l’en-tête). **Index stagé** : **`ListePlanningsOrganisation`**, **`ListePlanningsOrganisation.module.scss`**, **`PrintDocumentHeader`**, **`printPageStyles`**.

- **2026-06-06 (Liste enfants — impression personnalisée + commande `/maj`)** : **`ListePrintActions.tsx`** — bouton unique **Imprimer** ; si **`canPrint`** + **`canPrintCustomSelection`** : modale choix (**Liste filtrée** / **Sélection personnalisée**). **`ListeCustomPrintSelection.tsx`** : titre éditable, cases par ligne (données filtrées), second **`usePrintContent`**. **`Liste.tsx`** : props **`canPrintCustomSelection`**, **`printRowLabel`**, **`printRowKey`**. **`ListeEnfants`** : les deux modes activés. Styles **`printChoiceOption*`** (description lisible au survol). Commande Cursor **`.cursor/commands/maj.md`** → **`/maj`** (sync Memory Bank). **Index stagé** : 5 fichiers liste ; **`maj.md`** non commité.

- **2026-06-06 (Sanitaire — filtres dossiers & colonnes dissociées)** : **`DetailsSejourSanitaire`** — barre **`.optionsBar`** (style d’origine conservé) : **7 filtres** indépendants (**Allergies**, **Traitements**, **Contacts parents**, **Infos médicales & PAI**, **Alimentation**, **Autres infos**, **À prendre en sortie**) ; **« Autres infos »** et **« À prendre en sortie »** dissociés (type **`SanitaireColonnesOptionnelles`** : **`autresInformations`**, **`aPrendreEnSortie`** — remplace **`complements`**, migration **`localStorage`**). Case **Tout** (cocher/décocher tout, état **indéterminé**). **`ListeSanitaireDossiers`** : filtre lignes **OU** entre groupes cochés (**`ligneGardeePourGroupesColonnes`** → **`exigences.some`**) ; colonnes dynamiques par groupe ; **`listeKey`** + **`persistFiltersStorageKey`**. **`Liste.tsx`** : prop optionnelle **`initialHiddenColumnKeys`**. Fiches **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**. **Working tree** (non commité) : **`DetailsSejourSanitaire`**, **`ListeSanitaireDossiers`**, SCSS sanitaire.

- **2026-06-05 (Profil & Header — rôle séjour vs système)** : helper **`helpers/libelleRoleSurSejour.ts`** — **`roleUtilisateurSurSejour`** (directeur ou **`roleSejour`** équipe via snapshot **`HeaderSejourContextSnapshot`**) ; **`libelleRoleBadgeProfil`** : séjour en contexte (**`lireHeaderSejourContext`** / **`resolvedForHeader`**) → **`getRoleSejourByGenre`** ou **Directeur/Directrice** ; **aucun séjour** → **`getRoleSystemeByGenre`** (rôle système pour tous, y compris **BASIC_USER**). **Header** : lien Profil **`Prénom (rôle)`** pour tous les rôles. **Profil** : badge sous la photo (même helper). **`getRoleSystemeByGenre`** : **ADMIN** affiché **Admin** (plus « Administrateur »). Fiches **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Profil — refonte UX layout)** : **`Profil.tsx`** / **`Profil.module.scss`** — grille **col-lg-4 / 8** ; carte gauche **circulaire** (`border-radius: 50%`, ~**23.5rem**, sticky) : **photo** (~**78 %** / max **12.5rem** desktop, **10.5rem** mobile) + **badge rôle** uniquement (nom / email retirés de la carte) ; bouton caméra (UI, pas de mutation photo API). Colonne droite : **3 accordéons** (*Informations personnelles*, *Contact*, *Compte & statut*) — **1er ouvert** par défaut (`openSections = new Set([0])`) ; lignes compactes **label à côté de la value** ; édition champ par champ inchangée (**`canEditEmail`**, **`ChangePasswordForm`**). Fiches **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Impression listes — module `src/print/` complété)** : **`usePrintContent`**, **`PrintContentRoot`**, **`PrintTrigger`**, **`PrintDocumentHeader`**, **`PrintRunningHeader`**, **`printPageStyles`** — en-tête répété **Titre — n/N** via **`@page` margin boxes** (Chrome 131+, Safari 18.2+) ; **Firefox** : **`supportsPageMarginBoxes`** → mode **`fixed`** (titre répété, **sans numérotation**). Pas de pied de page ; **`size: A4`** non forcé par défaut (portrait/paysage = dialogue navigateur). Intégration **`Liste.tsx`** : **`canPrint`**, **`printHeaderContext`**, **`printValue`**, colonnes **`toggleable`**, bouton **Imprimer** sous cases colonnes. Consommateurs : **`ListeEnfants`**, **`TableauUtilisateurs`** (équipe séjour). Helpers **`libelleFiltresListeActifs`**, **`buildPrintDocumentContext`**. Fiches **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Liste enfants — impression, colonnes masquables, en-tête)** : module **`src/print/`** (`usePrintContent`, **`PrintContentRoot`**, **`PrintTrigger`**, **`PrintDocumentHeader`**, **`PRINT_GLOBAL_CLASS`**, **`libelleFiltresListeActifs`**, **`buildPrintDocumentContext`**) — intégré dans **`Liste.tsx`** via **`canPrint`** / **`printHeaderContext`** (contenu filtré, sans colonne actions ni ligne filtres). **`ColumnConfig.toggleable`** : cases à cocher en-tête pour masquer/afficher des colonnes (état local ; filtre de la colonne masquée effacé) ; en-tête **`.page_head`** — boutons **Effacer** / **Ajouter** groupés, cases à droite, **Imprimer** en dessous aligné à droite. **`ListeEnfants`** : **`canPrint`** ; colonnes masquables sauf **Nom** / **Prénom** ; genre affiché **Garçon** / **Fille** (**`libelleGenreEnfant`**, valeurs API inchangées). Fiches **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Tri personnes & sélection groupes — centralisation UI)** : **`helpers/trierUtilisateurs.ts`** — règle unique : affichage **« Prénom Nom »** → **`trierParPrenomPuisNom`** / **`comparerParPrenomPuisNom`** ; **« Nom Prénom »** → **`trierParNomPuisPrenom`** ; alias **`trierEnfantsParNom`** / **`trierEnfantsParPrenom`**. Modales équipe (activités, planning, cahier infirmerie, référents chambres, etc.) et filtres animateurs alignés. **`helpers/groupesParType.ts`** — **`SECTIONS_TYPE_GROUPE`**, **`groupesParSectionsType`**, **`trierGroupesParNom`**. **`SelectionGroupesParType.tsx`** (+ SCSS) : cases à cocher / **`<optgroup>`** / filtres déroulants par sections ; variantes **`picker`**, **`inline`**, **`planning`** (marge gauche cases planning groupe). Consommateurs : **`ListeActivites`**, **`ListeActivitesPrestataires`**, **`ListePlanningsOrganisation`** (cellule **GROUPE** + select ligne), **`ListeChambres`**, **`ListeActivitesCalendrier`**, **`ListeActivitesListe`**, **`ListeEnfants`**, **`TableauUtilisateurs`** ; **`AffichageGroupesListe`** délègue le regroupement. Fiches **`decisions-architecturales.md`**, **`etat-projet.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Header responsive & vue générale — UX layout)** : **`DetailsSejourOverview`** — bandeau **`.overviewInfos`** (description, lieu, dates, durée) **centré** ; accordéons **tous fermés** à l’arrivée (`openAccordions` initial **`[]`**, ouverture conservée via **`location.state.openAccordion`**). **Header** : menu burger dès **&lt; 1400px** (**`Navbar expand="xxl"`**, **`$header-nav-burger-max: 1399.98px`** dans **`Header.module.scss`**) ; hauteur mesurée en JS (**`helpers/siteHeaderHeight.ts`**, **`observeSiteHeaderHeight`**, **`ResizeObserver`**) → variable CSS **`--site-header-height`** sur **`document.documentElement`** ; **`body { padding-top: var(--site-header-height) }`** (**`styles.scss`**) — le contenu ne passe plus sous le header quand la nav revient à la ligne. **Largeur page** : retrait **`scrollbar-gutter: stable`** (bande blanche à droite sans scroll vertical) ; **`overflow-x: clip`** sur **`html`**, **`body`**, **`#root`** ; header fixe **`left: 0; right: 0`**, fond bleu sur **`.main`**. Fiches **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`contexte-actif.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Listes — pastilles groupes partagées)** : composant **`AffichageGroupesListe`** (`components/Liste/`) — affichage commun **`ListeEnfants`** + **`TableauUtilisateurs`** : pastilles compactes (style proche **`ListeChambres`** **`typeBadge`** / **`genreBadge`**) ; **couleur stable par `groupe.id`** via **`pastilleCouleursPourGroupe`** (`helpers/couleurGroupe.ts`, teinte angle d’or, fond clair + texte foncé + bordure) ; même **typeGroupe** sur une ligne, changement de type → nouvelle ligne ; **`texteFiltreGroupes`** pour filtres tableau ; **`SECTIONS_TYPE_GROUPE`** partagé avec modales édition. Fiches **`decisions-architecturales.md`**, **`etat-projet.md`**, **`documentation-ui-routing.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Vue générale — colonnes groupes / chambres sur listes enfants & équipe)** : **`detailsSejourLoader`** enrichi **`chambres`** (`getChambresDuSejour`). **`ListeEnfants`** : colonnes **Groupe(s)** / **Chambre** conditionnelles (≥ 1 groupe / chambre **ENFANT**) ; crayon → modales (checkboxes groupes par **typeGroupe**, radio chambre identifiant seul) ; **`sejourGroupeService`** / **`sejourChambreService`** ; helpers **`indexerAffectationsOccupants`**, **`chambreOccupantsUtils`**. **`TableauUtilisateurs`** / **`Equipe`** : colonnes **Groupe(s)** (référent, **`ajouterReferent`** / **`retirerReferent`**) et **Chambre** (**EQUIPE**, **`affecterMembreEquipe`** / **`retirerMembreEquipe`**) ; colonne **Rôle** (libellé **Rôle**, affichage **AS**, **SB**, **Anim**, **Adj**, **Autre**) ; largeurs colonnes (**`TableauUtilisateurs.module.scss`**, **`colAge`** fixe dans **`Liste.module.scss`**). **`Liste.tsx`** **`ColumnConfig`** : **`className`**, **`filterPlaceholder`** ; placeholders filtres compacts (**`styles.scss`**). Fiches **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**, **`etat-projet.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Sorties / prestataires — historique + UX non-participations)** : **`GET /sejours/{sejourId}/activites-prestataires/{id}/historique`** — **`sejourActivitePrestataireService.getHistoriqueActivitePrestataire`**, type **`HistoriqueModificationActivitePrestataireDto`** (**`type: ACTIVITE_PRESTATAIRE`**, snapshots `Nom: … | …`). UI fiche **`ListeActivitesPrestataires`** : bouton **Historique** (lecture **`ACCES_SEJOUR`**), **`HistoriqueModificationListeModal`** (**`formatSnapshots="activite_prestataire"`**), re-fetch si panneau ouvert après mutation ; **404** « Sortie introuvable ». **`libelleHistoriqueModification`** : non-participations en phrases par animateur avec **moments** (*« X ne participe pas à la sortie (Matin 1, …). »* / réintégration). **Exclusions** : nom animateur **une fois** + cases par moment (ordre paramétrage **`trierMomentsChronologiquement`**) — formulaire **Exclure** / **Ne participent pas** et **cartes** liste (**`grouperNonParticipationsParAnimateur`**, **`listeActivitesPrestatairesCalendrier.ts`**). Fiches **`etat-projet.md`**, **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-06-05 (Chambres — historique + UX filtres / formulaire)** : **`GET /sejours/{sejourId}/chambres/{chambreId}/historique`** — **`sejourChambreService.getHistoriqueChambre`**, type **`HistoriqueModificationChambreDto`** (**`type: CHAMBRE`**, snapshots `Type: … | …`). UI : bouton **Historique** sur chaque carte, **`HistoriqueModificationListeModal`** (**`formatSnapshots="chambre"`**), re-fetch si panneau ouvert après mutation ; diff occupants **Ajouté(e)(s) / Retiré(e)(s)** accordé au **`Genre`** chambre (**`libelleHistoriqueModification`**). **Filtre recherche** : **`normaliserPourRechercheTexte`**, **`textesRechercheChambre`** (**`libelleChambre.ts`**) — `bat` / `bat.` / `batiment` / accents / casse → localisation (**Bât. …**). **Formulaire** : étage chiffres uniquement ; retrait occupant → icône **`faUserMinus`**. Fiches **`etat-projet.md`**, **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-31 (Chambres — spec API ACCES_SEJOUR + alignement types)** : droits **lecture/écriture** = **`ACCES_SEJOUR`** (tout membre du séjour, plus **`peutGererMembresEquipeSejour`**) ; **`ChambreDto.groupe`** → **`GroupeResumeDto`** ; POST affectation → **`ChambreDto`** retourné (**`fusionnerChambreRetourneeDansListe`**) ; suppression prop **`peutGererChambres`**. Fiches **`etat-projet.md`**, **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-31 (Chambres — occupants + groupe + UX filtres, index stagé)** : **`ListeChambres.tsx`** — affectation **occupants** (enfants / membres d’équipe) : modale picker multi-sélection, recherche, jauge capacité, retrait occupant ; **`sejour-chambre.service.ts`** : **`affecterEnfant`** / **`affecterEnfants`**, **`retirerEnfant`**, **`affecterMembreEquipe`** / **`affecterMembresEquipe`**, **`retirerMembreEquipe`** (`POST/DELETE …/occupants/…`). **`ChambreDto`** enrichi : **`groupe`**, **`occupants`** ; **`SaveChambreRequest.groupeId`** (type **ENFANT**) ; types **`ChambreOccupantDto`**, **`AffecterOccupants*Request`**. Helper **`chambreOccupantsUtils.ts`** : éligibilité genre/groupe, index affectations, validation client PUT (**`analyserModificationChambreIncompatible`**) ; **`GenreChambreBadgeLabels`**. **`DetailsSejourChambres`** : **`enfants`** + **`groupes`** du loader **`sejour-detail`**, équipe avec **genre**. Filtres : recherche texte (identifiant, localisation, occupant), dropdown multi **groupes** ; pastilles genre/groupe ; mutations → **`rafraichirChambres`** (1 GET, état local — plus **`useRevalidator`**). Fiches **`etat-projet.md`**, **`decisions-architecturales.md`**, **`documentation-ui-routing.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-30 (Chambres — onglet séjour, CRUD + référents)** : route **`/mes-sejours/:id/chambres`** → **`DetailsSejourChambres`** + loader **`chambresLoader`** (`detailsSejourChambresLoader.ts`, **`GET /sejours/{sejourId}/chambres`**). Segment **Header** **Chambres** (`FaBed`) après **Sanitaire**. **`ListeChambres.tsx`** : cartes (identifiant principal + surnom optionnel), filtres type / genre / bâtiment, CRUD modales (pattern **`ListeLieux`**), référents animateurs sur type **`ENFANT`** via **`ReferentsSelector`** + sync **`ajouterReferent`** / **`retirerReferent`** (pattern **`CreateGroupeForm`**) ; type **`EQUIPE`** sans UI référents ; confirmation si passage **ENFANT → EQUIPE** avec référents existants. Écriture **`peutGererMembresEquipeSejour`** (aligné **`GESTION_SEJOURS`** API) ; lecture pour tous les participants. **`sejour-chambre.service.ts`** ; types **`ChambreDto`**, **`SaveChambreRequest`**, **`ChambreReferentInfos`**, enums **`TypeChambre`**, **`GenreChambre`** ; helper **`libelleChambre.ts`**. **Hors périmètre** : affectation occupants (enfants / équipe) — encart informatif en bas de page. Fiches **`documentation-ui-routing.md`**, **`etat-projet.md`**, **`decisions-architecturales.md`**, **`roadmap.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-30 (Auth — redirect login + retour après connexion)** : **401** ne affiche plus la page « Connexion requise » — redirect direct **`/`** via **`logoutAndRedirectToConnexion`**, **`navigateToRouteError`**, **`RedirectToConnexion`**, **`error-page`**, **`Erreur.tsx`**. **`helpers/cheminApresConnexion.ts`** : mémorisation **`sessionStorage`** **`enjoy.cheminApresConnexion`** (URL interne + query + hash ; exclus **`/`**, **`/erreur`**, URLs absolues) avant redirect login ; **`ProtectedRoute`**, **`Layout`** (401 profil). **`loginAction`** → **`chargerProfilEtCheminDestinationApresConnexion`** (consomme le chemin mémorisé après **`getUser`**, sinon **`cheminAccueilDepuisEtatActuel`**). Déconnexion menu : **`effacerCheminApresConnexion`**. Fiches **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-30 (Sorties / prestataires — calendrier activités + fiche dédiée)** : **`detailsSejourLoader`** charge **`activitesPrestataires`** (`sejourActivitePrestataireService.listerActivitesPrestataires`). Route **`/mes-sejours/:id/activites/sorties-prestas`** → **`DetailsSejourActivitesPrestataires`** / **`ListeActivitesPrestataires`** (pas de double GET au montage — données du loader parent). **`ListeActivites`** : cartes sortie sur le planning (référents des **`groupeIds`**), conflits sortie ↔ activité interne (modale direction ; animateur : message court, pas de création), retrait sortie / **`nonParticipations`** (poubelle réservée **`peutGererActivitesComplet`**), lien **Sorties / prestas** (flex, bleu). **`listeActivitesPrestatairesCalendrier.ts`** : fusion cellules, **`messageDoublonSortiePrestataire`** (date × moment × groupe, pré-validation + message API 400), **`lignesParticipationSortieFormulaire`**, conflits calendrier. **Fiche sorties** : filtre texte + date, formulaire (nom en premier, libellés gras), section **Ne participent pas** (liste + **Réintégrer**) + bouton **Exclure un animateur** (panneau cases à cocher des participants encore actifs). Types **`ActivitePrestataireDto`**, **`NonParticipationPrestataireDto`**, **`SaveActivitePrestataireRequest`**. Fiches **`documentation-ui-routing.md`**, **`etat-projet.md`**, **`decisions-architecturales.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-29 (Références alimentaires — UX catalogue Paramétrage)** : **`ListeReferencesAlimentaires`** — retrait affichage et saisie du champ **`ordre`** (tri technique API conservé : **`trierReferencesAlimentaires`**, **`prochainOrdreSuggest`** à la création, **`ordre`** inchangé à l’édition) ; espacement **`2.5rem`** entre sous-sections **Allergènes** et **Régimes et préférences** (**.subSection`**). Fiches **`decisions-architecturales.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-29 (Memory Bank — onglet Menus : réduction appels API)** : à l’ouverture de **`/mes-sejours/:id/menus`**, **3 à 4 GET** (dont **`getSejourById`** redondant + double fetch **`useEffect`** en Strict Mode). Correctifs : **`sejourDetailRouteCache.ts`** (**`mettreEnCacheSejourRoute`** dans **`detailsSejourLoader`**) ; **`menusLoader`** lit le séjour en cache → **`chargerDonneesMenusSejour`** = **2 appels** (`references-alimentaires-agregees-enfants` + `menus`) ; helpers **`menusSejourUtils.ts`** ; **`DetailsSejourMenus`** : **`useLoaderData`**, pas de chargement initial en **`useEffect`** ; après CRUD menu → **`rafraichirMenus`** (1 GET `menus` seulement). Fiches **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-29 (Memory Bank — connexion : triple appel API séjour corrigé)** : après login, **`detailsSejourLoader`** s’exécutait **3×** (10 endpoints × 3). Causes : revalidation React Router du loader **`/`** post-POST + **`redirect`** **`loginAction`** + **`<Navigate>`** client dans **`Connexion`**. Correctifs : **`connexionShouldRevalidate`** (`formMethod === "POST"` → `false`), suppression **`<Navigate>`** redondant, **`createBrowserRouter`** déplacé au niveau module dans **`App.tsx`**. Fiches **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-29 (Memory Bank — réunions préchargées + ordre accordéons Overview)** : **index stagé** (3 fichiers). **`detailsSejourLoader`** : **`sejourReunionService.listerReunions`** dans le **`Promise.all`** → champ **`reunions`**. **`DetailsSejourOverview`** : **`initialReunions={reunions}`** sur **`SectionReunionsSejour`** ; ordre accordéons — état initial via **`readOverviewAccordionOrder(sejourId)`** + sync **`useLayoutEffect`** (évite flash ordre par défaut). **`SectionReunionsSejour`** : prop **`initialReunions?`** — liste triée à l’init, **`chargement=false`**, pas de fetch au montage si préchargé ; mutations conservent **`recharger`**. Fiches **`decisions-architecturales.md`**, **`etat-projet.md`**, pivot **`AI_MEMORY.md`**.

- **2026-05-29 (Memory Bank — gestion centralisée des erreurs routes / API)** : **index stagé** (~29 fichiers). Helper **`helpers/routeError.ts`** : **`classifyApiError`**, **`throwRouteLoaderError`**, **`navigateToRouteError`**, **`FORBIDDEN_ROUTE_ERROR`**, messages FR **401** / **403** (ignore textes Spring). UI **`ErreurAffichage`**, **`Erreur.tsx`** (`/erreur`), **`error-page.tsx`** (boundary) ; catch-all **`*`** → 404. Loaders migrés (**`mesSejoursLoader`**, **`profilLoader`**, listes admin, **`detailsSejourLoader`**, **`dossierEnfantLoader`**, **`menusLoader`**, **`sanitaireLoader`**) — plus de listes vides / **`Error`** inline. **`ProtectedRoute`** : chargement si rôle absent, **403** via **`ErreurAffichage`**. **`redirectApresAuthentification`** : plus de fallback **`/profil`** si **`getUser`** échoue. **`axiosError.ts`** : **`NETWORK_ERROR_MESSAGE`**, **`isNetworkError`**. **`Layout`** : **`/erreur`** sans header ; échec profil → **`/erreur`**. Suppression **`Acces_non_autorise.tsx`**. Loaders dédiés **Menus** / **Sanitaire**. Fiches pivot **`AI_MEMORY.md`**, **`documentation-ui-routing.md`**, **`decisions-architecturales.md`**.

- **2026-05-29 (Memory Bank — restriction modification email utilisateur)** : **`PUT /utilisateurs`** inchangé en signature ; le backend refuse le changement d’email selon le rôle connecté / cible. Front : helper **`helpers/canEditEmail.ts`** (`canEditEmail`, **`getEmailReadOnlyMessage`**) ; **`Profil.tsx`** — email éditable **ADMIN** uniquement en auto-modification, message sous le champ (classe **`.withHint`**), payload conserve **`initialUtilisateur.email`** si lecture seule, erreurs via **`getApiErrorMessage`** (403 / 409 / 404) ; **`UserForm.tsx`** — email conditionnel (**admin** toujours ; **directeur** sur membre d’équipe **`BASIC_USER`** ≠ soi) + **`updateUser`** avant **`modifierRoleMembreEquipe`** en édition équipe ; **`role`** / **`dateExpirationCompte`** non exposés hors admin. **`tokenId`** obligatoire (JWT **`sub`** + Redux **`role`**). Working tree : **`canEditEmail.ts`**, **`Profil`**, **`UserForm`**.

- **2026-05-24 (Memory Bank — comptes rendus de réunion, vue générale)** : **`HEAD`** **`3ae7d47`** ; évolutions **`SectionReunionsSejour.tsx`** (+ **`SectionReunionsSejour.module.scss`**), **`ReunionFormulaireModal.tsx`**, **`ReunionContenuRichText.tsx`**, **`helpers/reunionTiptapExtensions.ts`**, **`helpers/reunionTipTapTextePourRecherche.ts`**, **`services/sejour-reunion.service.ts`**, **`api.d.ts`** (**`ReunionDto`**, **`SaveReunionRequest`**, **`ReunionContenuTipTapJson`**) ; **`styles.scss`** curseurs champs vs TipTap. **UI** : accordéons par date (**tri date ↓** puis **`id`**), entête avec ordre du jour à côté de la date, filtres ligne (**date** exacte ISO + recherche **ODJ / contenu** extrait TipTap), bouton **+ Nouveau compte rendu**, modale succès après enregistrement ; liste vide sans doublon CTA création ; **TipTap** : focus champs natifs vs éditeur (`setEditable`, `pointer-events`). **Synthèse pivot** : **`AI_MEMORY.md`**. À figer après **`git`** / push.

- **2026-05-17 (Memory Bank — cahier d’infirmerie, Sanitaire)** : **Git `HEAD`** **`3a97c6e`** ; **working tree / index** : **`DetailsSejourSanitaire`**, **`ListeCahierInfirmerie`**, **`CahierInfirmerieForm`** (+ SCSS), **`cahier-infirmerie.service.ts`** (`GET/POST/PUT/DELETE …/cahier-infirmerie`, historique par entrée), **`droitsCahierInfirmerie`**, **`cahierInfirmerieLabels.ts`** (**`SERUM_PHYSIOLOGIQUE`** → libellé **Serum physiologique**), **`api.d.ts`** (**`CahierInfirmerieEntreeDto`**, **`SaveCahierInfirmerieEntreeRequest`**, **`dateHeure`** `string | number` selon JSON, **`temperatureCelsius`**, **`HistoriqueSnapshotDomaine`** côté UI = **`cahier_infi`** via modal). **Liste** : colonne Soins — **Autre** / **Appel Autre** n’affichent que le **texte de précision** (pas le mot « Autre ») ; **Prise de température** avec unité **°C** ; dates **`formatDateHeureHistorique`** + **`parseDate`** (**secondes vs millisecondes**). **Historique** : **`formaterComparaisonHistoriqueSnapshots(..., "cahier_infi")`** — pas le parseur « activité » (8 tubes `|`) ; fusion **Soins + température** en une ligne lisible ; création : **`Champ : valeur`** sans préfixe **Vide →** sur chaque ligne ; modification : **uniquement champs modifiés**. **`Liste.tsx`** : ajustements styles si présents. À committer / pousser : recouper **`git diff`** avant de figer la Memory Bank.

- **2026-05-11 (Memory Bank — planning : « ma présence » & modale membres animateur)** : **Git `HEAD`** **`a14e3e5`** ; évolutions **`ListePlanningsOrganisation.tsx`** (**`ListePlanningsOrganisation.module.scss`**) — sans **`peutGererPlanningStructure`** et contenu **`MEMBRE_EQUIPE`** : liste modale **`membresListeModaleCelluleMembreEquipe`**, autres noms lisibles grisées (**`membreCheckboxLabelMuted`**) ; enregistrement via **`modifierMaPresenceCelluleMembreEquipe`** (**`PATCH …/ma-presence`**, **200** ou **204**). **`DetailsSejourOrganisation.tsx`** passe **`tokenUtilisateurConnecte`**. **`sejour-planning-grille.service.ts`** + **`api.d.ts`**. Alignement **`enjoyApi/docs/ai/documentation-api-rest.md`** (puce Front sous **PATCH ma-presence**).

- **2026-05-10 (Memory Bank — alignement `.cursorrules` / pivot `#MAJ` : historique & calendrier activités)** : **Git `HEAD`** **`89f4f4d`** ; **working tree** avec travail **non commité** sur historique API + UI (liste des fichiers : `git status`). **API** — **`GET /sejours/{id}/activites/{activiteId}/historique`**, **`GET …/planning-grilles/.../historique`** (services **`sejourActiviteService.getHistoriqueActivite`**, **`sejour-planning-grille`**). **Types** — **`HistoriqueModificationActiviteDto`**, **`HistoriqueModificationPlanningCelluleDto`**, etc. dans **`api.d.ts`**. **UI** — **`HistoriqueModificationListeModal`** (domaines **activite** / **planning_cellule**) ; **`libelleHistoriqueModification`** (actions, **`formatDateHeureHistorique`** → **`parseDate`** dans **`formaterDate.ts`** pour timestamps numériques / secondes) ; affichage **« Vide »** pour valeur vide dans les lignes de comparaison. **Liste / calendrier activités** — icône historique sur cartes et cartes liste ; **planning organisation** — icône par cellule + modal sans lien `ligne #`. **Calendrier** — **`requestDeleteActivite(id, { animateurTokenId, dateYmd })`** ; après suppression, **`rappelHistoriqueApresSuppression`** avec coordonnées case → bandeau **`calendrierCellRappelSuppression`** dans le **`td`** (styles **`ListeActivites.module.scss`**), **`onConsulterHistoriqueRappelSuppression`** / fermeture. Vérifier côté API que l’historique d’une activité **supprimée** reste accessible par id.

- **2026-05-08 (Memory Bank — `#MAJ` : écran Sanitaire séjour)** : **Git `HEAD`** **`a9c2635`** (**`origin/master`** à jour) ; **working tree** avec travail **non commité** : **`App.tsx`**, **`Header.tsx`**, **`DetailsSejourSanitaire`** (+ SCSS), **`ListeSanitaireDossiers`**, **`Liste.tsx`**, **`DossierEnfant.tsx`**, **`sejour-enfant.service.ts`**, **`api.d.ts`**. **Route** **`/mes-sejours/:id/sanitaire`** (arbre **`sejour-detail`**), segment **Header** **Sanitaire** (`FaNotesMedical`) entre **Menus** et **Paramétrage**. **API** **`GET /sejours/{sejourId}/dossiers-enfants`** via **`listerDossiersEnfantsSanitaire`** ; types **`GroupeResumeDto`**, **`EnfantDossierSanitaireLigneDto`**. **UI** : cases colonnes optionnelles, préférences **`localStorage`** **`enjoy.sanitaire.opts.{sejourId}`** ; **`ListeSanitaireDossiers`** compose **`Liste`** avec colonnes dynamiques et filtre métier (si groupes de colonnes affichés : lignes avec données dans chaque groupe coché, **ET** entre groupes). **`persistFiltersStorageKey`** sur **`Liste`** → filtres **`sessionStorage`** **`enjoy.liste.filters.{clé}`**. **`DossierEnfant`** : **`state.from === 'sanitaire'`** → **Retour** vers **`/mes-sejours/:id/sanitaire`**.

- **2026-05-07 (Memory Bank — `#MAJ` : dossiers enfants — édition par section + permissions AS)** : **Git `HEAD`** **`d11fc65`** (**`origin/master`**), **index stagé** (5 fichiers modifiés/créés). **Édition par section** — **`DossierEnfant.tsx`** : suppression du bouton « Modifier » global en haut du dossier ; ajout d'un **bouton d'édition par section** (icône crayon, taille `sm`, classe `.sectionEditButton`) dans un nouveau `.sectionHeader` (flexbox titre + bouton). Sections : **Contacts parents**, **Informations médicales**, **Traitements**, **Autres informations**. État `editingSection: SectionType | null` (types `'contacts' | 'medical' | 'traitements' | 'autres'`), fonction **`getSectionTitle()`** pour le titre modal dynamique. **`DossierEnfantForm.tsx`** : prop **`section?: SectionType`**, constante **`sectionFieldsMap`** associe chaque section à ses champs ; si `section` fourni, filtre les `fields` pour n'afficher que ceux de la section concernée (permet édition ciblée sans charger tous les champs). **Permissions AS** — nouveau helper **`helpers/peutModifierDossierEnfant.ts`** : autorise **directeur du séjour**, **adjoint** (`RoleSejour.ADJOINT`), et **AS** (`RoleSejour.AS` = Assistant sanitaire) à modifier les dossiers enfants (logique métier : AS gère les infos médicales, traitements, allergènes). **`DossierEnfant.tsx`** utilise `peutModifierDossierEnfant` au lieu de `peutGererMembresEquipeSejour`. **Alignement formulaire** — **`Form.module.scss`** : labels centrés verticalement (`align-items: center` au lieu de `end`) pour meilleure présentation des champs (particulièrement les textarea). **`DossierEnfant.module.scss`** : ajout styles `.sectionHeader` (flex space-between), `.sectionTitle` (marge ajustée), `.sectionEditButton` (padding compact, effet hover scale). Fichiers modifiés : **`DossierEnfant.tsx`**, **`DossierEnfantForm.tsx`**, **`Form.module.scss`**, **`DossierEnfant.module.scss`**, nouveau **`peutModifierDossierEnfant.ts`**. Aucune erreur lint.

- **2026-05-07 (Memory Bank — `#MAJ` : redirection après connexion & dernier séjour)** : **Git `HEAD`** **`002d590`** (**`origin/master`**), working tree clean. Persist **localStorage** **`enjoy.dernierSejourId.{sub}`** (JWT **`payload.sub`**) lors du chargement d’un **`/mes-sejours/:id`** dans **`Header.tsx`** (**`sejour-detail`** loader) ; **conservé** après **`logout`** (le snapshot header reste **`sessionStorage`**, vidé au logout comme avant). **`redirectApresAuthentification.ts`** : **`chargerProfilEtCheminAccueil`** (`getUser` + **`/mes-sejours/:id`** si **DIRECTION** / **BASIC_USER** et id valide lu ; sinon **`/profil`**), **`cheminAccueilDepuisEtatActuel`** pour redirection sync. **`loginAction`** + loader **`/`** (**`App.tsx`**) utilisent **`chargerProfilEtCheminAccueil`** ; **`Connexion`** **`Navigate`** si déjà connecté. **ADMIN** → **`/profil`**. Fiches pivot **`AI_MEMORY.md`**, **`documentation-ui-routing.md`** synchronisées.

- **2026-05-07 (Memory Bank — `#MAJ` : usages de lieux)** : **Git `HEAD`** **`002d590`** (**`origin/master`**), **index stagé**. **Enum `UsageLieu`** (`src/enums/UsageLieu.ts`) : **`ACTIVITE`**, **`SURVEILLANCE`**, **`RASSEMBLEMENT`**. Types **`api.d.ts`** : **`LieuDto.usages: UsageLieu[]`** et **`SaveLieuRequest.usages`** (obligatoire, non vide). **`ListeLieux.tsx`** : formulaire ajout/modif — checkboxes multi-sélection usages (validation ≥ 1 usage) ; affichage usages sur cartes lieux. **`ListeActivites.tsx`** : filtrage automatique `lieuxFiltrésParEmplacement` → garde uniquement lieux avec usage **`ACTIVITE`** ; modal activité — filtre emplacement lieu **inline** à côté label « Lieu (optionnel) », libellé « Filtrer les lieux », option « Tous » au lieu de « Tous les emplacements », select `bsSize="sm"`. **`ListePlanningsOrganisation.tsx`** : variable **`lieuxPourPlannings`** filtre lieux → usages **`SURVEILLANCE`** | **`RASSEMBLEMENT`** ; utilisée pour `donneesPourTypesPlanningMeta`, `optionsPourSource`, affichage libellés lignes, cellules, modal cellule (message si aucun lieu avec usage planning). Styles : **`ListeLieux.module.scss`** / **`ListeActivites.module.scss`** — classe **`.checkboxRow`** (espacement checkbox ↔ label `0.5rem`) ; **`.lieuHeaderRow`** / **`.lieuFilterInline`** / **`.lieuFilterLabel`** / **`.lieuFilterSelect`** (filtre inline activités). Alignement **`enjoyApi/docs/ai/documentation-api-rest.md`** : règle backend — source planning **`LIEU`** impose usages **`SURVEILLANCE`** | **`RASSEMBLEMENT`** ; source activité **`lieuId`** valide avec usage **`ACTIVITE`**.

- **2026-05-06 (Memory Bank — `#MAJ` : droits gestionnaires séjour)** : **Git `HEAD`** **`cbe342b`** (**`origin/master`**) ; **index stagé** — **`peutGererMembresEquipeSejour`**, **`Header`** (masquage onglet **Paramétrage**), **`DetailsSejourParametrage`** (**`Navigate`** hors directeur/adjoint), écrans **Overview / Activités / Menus / Organisation / dossier enfant**, **`ListePlanningsOrganisation`** (libellé **Consulter le planning**), **`ParametrageAffichageMenus`** **`modifiable`**, **`ListeReferencesAlimentaires`** (en‑tête section), **`PlanningCalendrier`** carte **lecture seule** détail dans le pivot **`AI_MEMORY.md`**. **`documentation-ui-routing.md`** et **`decisions-architecturales.md`** synchronisés (Paramétrage réservé, ids accordéons **1–6**).

- **2026-05-06 (mise à jour Memory Bank + messages API)** : **`.cursorrules`** — *Qualité* / *Formulaires* — **`getApiErrorMessage`**, **`errorMessagePlacement`** ; **`axiosError`** / **`Form`** / dossier groupe import (voir synthèse pivot *antérieurement commit **`cbe342b`***). **Réflexivité :** anciennes lignes peuvent encore citer **`/directeur/sejours`** alors que **`App.tsx`** utilise **`/mes-sejours`**.

- **2026-05-05 (Suppression — remontée des erreurs API jusqu’à l’UI) :** Cohérence du flux de suppression dans toute l’UI : quand le backend refuse une suppression (ex. **403 `{"error":"Access Denied"}`** sur **`DELETE /sejours/{id}/equipe/{membreTokenId}`** pour un utilisateur sans `ROLE_DIRECTION`, ou **409** métier), l’erreur est désormais **toujours** affichée à l’utilisateur et la modale de succès n’apparaît plus à tort. **`Liste.tsx`** : `deleteModalState` enrichi avec **`errorMessage`** + **`isDeleting`** ; `confirmDelete` bascule en **`isDeleting=true`** pendant l’appel, conserve la modale ouverte sur erreur, renseigne `errorMessage` à partir de **`error.message`** (déjà extrait par **`adaptAxiosError`** depuis `response.data.error` ou `response.data.message`), avec fallback générique. **Modale** : encart rouge **`deleteErrorMessage`** (style ajouté à **`Liste.module.scss`**), bouton **Confirmer** désactivé / libellé « Suppression... » pendant l’appel, bouton **Annuler** renommé **Fermer** dès qu’une erreur s’affiche. **Wrappers `onDelete` corrigés** pour propager l’exception au composant `Liste` (auparavant ils l’avalaient dans `console.error`, ce qui faisait croire à une suppression réussie) : **`Equipe.tsx`** (`handleDeleteMembre`), **`TableauUtilisateurs.tsx`** (`handleDefaultDelete`), **`ListeEnfants.tsx`** (`handleDeleteEnfant`). `ListeSejoursAdmin` faisait déjà `throw error` ; il bénéficie maintenant aussi de l’affichage. Détail backend des privilèges DELETE équipe / utilisateur : voir **`enjoyApi/docs/ai/decisions-architecturales.md`** (privilèges `Role` / `RoleSejour`, `CustomAccessDeniedHandler`).

- **2026-05-05 (Séjours par utilisateur — endpoint élargi & renommages) :** Suite à la modif backend (`enjoyApi/docs/API_CHANGEMENT_SEJOURS_PAR_UTILISATEUR.md`), passage de **`GET /sejours/directeur/{tokenId}`** (DIRECTION) à **`GET /sejours/utilisateur/{utilisateurTokenId}`** (ADMIN, **DIRECTION**, **BASIC_USER**). **`sejour.service.ts`** : `getAllSejoursByDirecteur` → **`getAllSejoursByUtilisateur`** (+ message d’erreur générique). **Renommage UI** : dossier **`src/Pages/_Directeur/`** → **`src/Pages/_Sejours/`** (pages partagées DIRECTION + BASIC_USER membre d’équipe) ; **`ListeSejoursDirecteur/ListeSejoursDirecteur.tsx`** → **`MesSejours/MesSejours.tsx`** ; loader **`listeSejoursDirecteurLoader`** → **`mesSejoursLoader`**. **Routes** : **`/directeur/sejours[...]`** → **`/mes-sejours[...]`** dans **`App.tsx`** ; **`ProtectedRoute`** des routes séjour passe à **`[RoleSysteme.DIRECTION, RoleSysteme.BASIC_USER]`**. **`Header.tsx`** : drapeau **`isParticipantSejour`** (DIRECTION ou BASIC_USER) pour afficher la nav **Mes séjours** + segments du fil de séjour ; tous les **`useMatch`** / liens internes alignés sur **`/mes-sejours/:id*`**. Navigations internes (`ListeGroupes`, `ListeEnfants`, `DossierEnfant`, `DetailsSejour*`) toutes basculées sur **`/mes-sejours/...`**. Import scss de **`DetailsSejourAccordionItem`** mis à jour (chemin **`_Sejours/DetailsSejour/DetailsSejour.module.scss`**). L’API filtre selon le rôle : ADMIN (qui n’utilise pas cette route) verrait tous les séjours, DIRECTION/BASIC_USER ne voient que ceux où ils sont directeur ou membre d’équipe.

- **2026-05-05 (Suppression de groupe — affichage message backend) :** **`ListeGroupes.tsx`** : lors d'un échec de suppression (ex. groupe encore affecté à des activités, backend renvoie **HTTP 409** avec message explicite), le message d'erreur du backend est maintenant extrait et affiché directement dans la **modal de suppression** (au lieu d'un message générique). Le helper **`adaptAxiosError`** (`helpers/axiosError.ts`) extrait automatiquement `response.data.error` ou `response.data.message`. L'affichage d'erreur global dans l'accordéon a été retiré pour ne garder que l'affichage contextuel dans la modal. Style **`.errorMessage`** réutilisé depuis **`ListeGroupes.module.scss`**. **`sejour-groupe.service.ts`** : ajout du type de retour explicite `Promise<void>` sur `supprimerGroupe`.

- **2026-05-04 (Memory Bank — pivot & routing doc) :** **`AI_MEMORY.md`** : synthèse **dossier enfant** enrichie (chemin **`/directeur/sejours/:id/enfants/:enfantId/dossier`**, **`dossierEnfantLoader`**, affichage **`ReferenceAlimentaireDto`**, retour **`location.state`**). **`documentation-ui-routing.md`** : paramètre séjour **` :id`** aligné sur **`App.tsx`** pour la route dossier.

- **2026-05-04 (Menus — préférences d’affichage & Paramétrage) :** **`DetailsSejourParametrage.tsx`** : accordéons **`1`–`6`** (ajout **`5`** *Références alimentaires* — **`ListeReferencesAlimentaires`**, **`6`** *Affichage des menus* — **`ParametrageAffichageMenus`**) ; styles partagés **`DetailsSejour.module.scss`**. Persistance navigateur **`enjoy.detailsSejour.menuAffichage.{sejourId}`** via **`helpers/menuRepasAffichageSejour.ts`** (types de repas visibles, lignes de composition entrée / plat / fromage ou entremet / dessert) ; **`DetailsSejourMenus.tsx`** : lecture initiale + écoute **`enjoy-menu-affichage-changed`** pour refléter les changements sans recharger la route. Compléments **`helpers/menuRepas.ts`** pour la visibilité des champs de composition.

- **2026-05-04 (Menus séjour → PlanningCalendrier + factorisation, 3ᵉ commit) :** **`DetailsSejourMenus.tsx`** et **`DetailsSejourMenus.module.scss`** s’appuient sur **`PlanningCalendrier`** (cartes, cases ajout, footer modal, clavier). Corps de formulaire extrait en **`MenuRepasFormulaireCorps`** ; logique / libellés / couleurs / construction payload dans **`helpers/menuRepas.ts`**.

- **2026-05-04 (Liste des activités → PlanningCalendrier, 2ᵉ commit) :** **`ListeActivites.tsx`**, **`ListeActivitesCalendrier.tsx`** et **`ListeActivites.module.scss`** passent sur les composants et styles **`PlanningCalendrier`** (cartes édition/suppression, indications d’ajout sur cases vides, pied de modal **`PlanningModalFooterFormulaire`** où c’est factorisé, **`proprietesTdAjoutPlanning`** / clavier sur les **`td`** ajout).

- **2026-05-04 (PlanningCalendrier — primitives seules, premier commit) :** Ajout du dossier **`src/components/PlanningCalendrier/`** : **`CalendrierCarteEditionAvecSuppression`**, **`CalendrierCelluleAjoutHint`**, **`PlanningModalFooterFormulaire`**, **`calendrierCelluleClavier.ts`**, **`PlanningCalendrier.module.scss`**. Objectif : mutualiser UI + a11y des tableaux « planning » (cartes cliquables, cases vides ajoutables au clavier, pied de modal). **Les écrans consommateurs** (ex. **`ListeActivitesCalendrier`**, **`DetailsSejourMenus`**) peuvent suivre dans des commits séparés.

- **2026-05-04 (Menus repas & références alimentaires) :** Route **`/directeur/sejours/:id/menus`** dans **App.tsx** → **`DetailsSejourMenus`** pour la gestion des menus du séjour ; **`DetailsSejourMenus.module.scss`**. **Header** : segment **Menus** (`FaUtensils`) après **Activités**, avant **Paramétrage**. **`ListeReferencesAlimentaires`** (+ module SCSS) pour le référentiel (intégration ultérieure sous **Paramétrage**, accordéon **5** — voir entrée *préférences d’affichage* ci‑dessus). Services **`references-alimentaires.service.ts`**, **`sejour-menu.service.ts`** ; helpers **`dateIsoLocal.ts`**, **`optionsReferencesAlimentaires.ts`** ; **`api.d.ts`** mis à jour. **Formulaire dossier enfant** : **`DossierEnfant`**, **`DossierEnfantForm`**, évolutions **`Form.tsx`** / **`Form.module.scss`** pour les données diététiques. **`DetailsSejourParametrage`** ajusté en cohérence avec la nouvelle section.

- **2026-04-30 (titres avant contenu — vue générale, paramétrage, organisation) :** **`DetailsSejourOverview`** et **`DetailsSejourParametrage`** : `<h1>` (**Vue générale** / **Paramétrage**) au-dessus des accordéons, classe **`overviewSectionTitle`** (**`DetailsSejour.module.scss`**, même **`$police_titres`** / taille que **`page-title`** global utilisé par **`Liste`**). **`ListePlanningsOrganisation`** (liste `/organisation`) : titre **Organisation** en **`page-title`**, ligne d’actions **`actionsContainer`** (`flex-start`, **`gap`** titre ↔ bouton **Créer un planning**).

- **2026-04-29 (Organisation — page dédiée & grille fenêtrée) :** Les **plannings organisation** ne sont plus dans l’accordéon **`10`** de la vue générale : page **`DetailsSejourOrganisation`** sous **`/directeur/sejours/:id/organisation`** (liste + création) et **`/directeur/sejours/:id/organisation/:grilleId`** (éditeur tableau **pleine page**, bouton retour liste). **`App.tsx`** : route parent **`organisation`** → **`DetailsSejourOrganisationLayout`** (`pageContainer` + erreurs loader + **`Outlet`**) ; enfants **`index`** et **`:grilleId`** → même **`DetailsSejourOrganisation`** (`useParams`, props **`embeddedEditorGrilleId`**, **`onNavigateToPlanning`**, **`onCloseEmbeddedEditor`** vers **`ListePlanningsOrganisation`**). **Header** : segment **Organisation** (`FaTable`) entre Vue générale et Activités. **`ListePlanningsOrganisation`** : **`renderEditorSurface()`** partagé modale / page ; vue tableau sans scroll horizontal principal ; toolbar **1 j. / 3 j. / 7 j.** + **`CalendrierNavigationPeriode`** ; hook **`useCalendrierFenetreJours`** (bornes **`bornesDebutFenetreCalendrier`**, etc. depuis **`listeActivitesUtils`**) ; **`ListeActivites.tsx`** refactoré pour utiliser ce hook. SCSS : largeurs fixes colonnes **Actions** (9rem, sticky droit) et **libellé** (13rem dans **`.planningGridWindow`**).

- **2026-04-29 (détail séjour — Paramétrage & accordéon partagé) :** les blocs **Lieux**, **Moments**, **Horaires** et **Types d’activité** quittent la vue générale pour une page dédiée **`DetailsSejourParametrage`** (`/directeur/sejours/:id/parametrage`, même loader **`sejour-detail`**). **Header** directeur : segments **Vue générale** → **Organisation** → **Activités** → **Paramétrage** (`FaSlidersH`). **Vue générale** (`DetailsSejourOverview`) : accordéons **`1`–`4`** uniquement ; **plannings organisation** sur **`/organisation`** ; moments / horaires pour les plannings lus depuis le **loader** (révalidation après mutations côté Paramétrage via **`useRevalidator`**). Ordre accordéons Paramétrage persisté : `enjoy.detailsSejour.parametrageAccordionOrder.{sejourId}` ; à l’arrivée sur Paramétrage, **tous les accordéons fermés**. Composant **`DetailsSejourAccordionItem`** dans **`src/components/DetailsSejour/`** (réutilisé vue générale + paramétrage). Mise à jour **Memory Bank** + **`.cursorrules`** (routes / navigation).

- **Dernière vérification cross-projet :** 2026-03-31 (lecture `../../../enjoyApi/AI_MEMORY.md` — lieux partageables, occupation lieu / `avertissementLieu`, `lieuId` activités). **2026-04-03 :** moments. **2026-04-04 :** **types d’activité** (CRUD + lien obligatoire sur les activités), aligné `enjoyApi` (`TypeActivite`, `/types-activite`). **2026-04-22 :** synchronisation doc front (Header, liste activités, `ListeTypesActivite` / revalidation) ; implémentation **horaires** séjour (accordéon vue générale, CRUD, tri chronologique). **2026-04-23 :** plannings **organisation** (grilles direction, `ListePlanningsOrganisation`, `sejour-planning-grille`, panneau accordéon `10`, loader `planningGrilles`). **2026-04-24 :** plannings — **`MEMBRE_EQUIPE`** autorisé comme **type de libellé de ligne** (UI + `libelleUtilisateurTokenId`).

- **2026-04-28 :** **Plannings organisation** — modale **créer / modifier** grille : pour **type de libellé des lignes** et **type du contenu des cellules**, un `PlanningLigneLibelleSource` sans données côté séjour (lists **horaires**, **moments**, **groupes**, **lieux**, **équipe+directeur**) est affiché comme **option désactivée**, style **`planningMetaOptionIndisponible`**, avec libellé `Type — …` via **`noteCreerDabordPourTypePlanning`** (helpers **`sourcePlanningADonneesPourType`**, **`donneesPourTypesPlanningMeta`**). La valeur déjà choisie pour une grille existante reste sélectionnable même si les listes ont été vidées. Modale **nouvelle ligne / modifier ligne** : retrait du message d’avertissement sous le select quand **`entiteChoicesLigne`** est vide (redondant avec le flux création grille).
- **2026-04-28 (suite) :** **Cellules de grille** alignées sur le contrat API **plusieurs références par cellule** (`horaireIds` / `horaireLibelles`, `momentIds`, `groupeIds`, `lieuIds`, `membreTokenIds` — plus de champs singuliers au niveau cellule) ; types **`PlanningCelluleDto`** / **`PlanningCellulePayload`** dans **`api.d.ts`** ; modale cellule : **cases à cocher** multi-sélection (hors saisie libre) ; **`SAISIE_LIBRE`** : texte libre et/ou membres optionnels ; **affichage tableau** : une valeur par **ligne visuelle** (`join("\n")` + **`white-space: pre-line`** sur le span). **Création** planning : champ **consigne globale** en **une seule ligne** (`Input` texte) ; **édition** conserve le **textarea** multi-lignes. Doc API de référence côté backend : **`enjoyApi/docs/frontend-planning-cellules-multiples.md`**.

- **Dernière mise à jour de la Memory Bank :** 2026-06-07 (activités — enfants participants ; journal ci‑dessus)

## Dernières réalisations (synthèse)

- **`PlanningCalendrier/`** — primitives ; consommateurs **`ListeActivites*`** et **`DetailsSejourMenus*`** (+ **`MenuRepasFormulaireCorps`**, **`menuRepas.ts`**).

- **Menus repas (détail séjour)** — **`DetailsSejourMenus`** + **`DetailsSejourMenus.module.scss`** ; route **`/mes-sejours/:id/menus`**. **Référentiel** : **`ListeReferencesAlimentaires`** sur **`DetailsSejourParametrage`** (accordéon **5**) — CRUD allergènes / régimes, **`ordre`** API masqué en UI ; **préférences d’affichage** (accordéon **6**) : **`ParametrageAffichageMenus`**, **`menuRepasAffichageSejour.ts`**, event **`enjoy-menu-affichage-changed`**. Services **`sejour-menu.service.ts`**, **`references-alimentaires.service.ts`** ; helpers **`dateIsoLocal`**, **`optionsReferencesAlimentaires`** ; contrat **`api.d.ts`**. **Dossier enfant** : champs diététiques via **`DossierEnfant`**, **`DossierEnfantForm`**, **`Form`** / **`Form.module.scss`**.

- **Plannings organisation (direction)** — **`ListePlanningsOrganisation.tsx`** + **`ListePlanningsOrganisation.module.scss`** ; service **`sejour-planning-grille.service.ts`** (`/sejours/{sejourId}/planning-grilles` : lister grilles, détail, créer / modifier / supprimer grille, CRUD lignes, cellules). **Loader** : **`detailsSejourLoader.ts`** charge **`planningGrilles`** (try/catch si API indisponible). **Accès UI** : page **`/mes-sejours/:id/organisation`** (liste / création) et **`…/organisation/:grilleId`** (éditeur pleine page) ; les plannings ne sont plus sur la vue générale (accordéon **`10`** retiré). **Création planning** : navigation vers **éditeur** (`onNavigateToPlanning`) ou ancienne modale si callbacks absents ; **consigne globale** : **une ligne** en création, **textarea** en modification. **Cellules** : **plusieurs références** par cellule (DTO / payload tableaux **`…Ids`**, **`horaireLibelles`**) ; modale cellule en **cases à cocher** multi-sélection ; affichage **une valeur par ligne** dans la case (`pre-line`). **Tableau** (modale éditeur) : en-têtes de jours au **même format que le calendrier des activités** (`libelleJourCourtPourBouton` / `parseYmdVersDateLocale` depuis **`listeActivitesUtils.ts`**). **Sections** : libellés de regroupement **horizontaux** ; poignée de **déplacement de section entière** à **gauche** du texte (flex row, retours à la ligne) ; réordonnancement par blocs (`decomposerEnBlocsOrdre`, `reorderBlocsAvecCile`, `appliquerDeplacementSectionEntreBlocs`) — **ordre interne des lignes d’une section inchangé**. **Sections** listées dans l’ordre d’apparition (`sectionsDepuisLignes`). **`prochainOrdreNouvelleLigne`** : si aucune ligne « hors section » et nouvelle ligne sans regroupement, **`ordre = max(tous les ordres) + 1`** (évite doublon d’`ordre` quand toutes les lignes existantes sont dans une section). **Sans colonne libellé de ligne** (`sourceLibelleLignes == null` / `grilleLibelleLignesDesactive`) : **« Ajouter une ligne »** appelle **`creerLigneSansModal`** (pas de modale). **Glisser-déposer lignes** : même section uniquement ; **types** `PlanningGrilleDetailDto`, `PlanningLigneDto`, `SavePlanningLigneRequest`, etc. dans **`api.d.ts`** ; libellés enum **`PlanningLigneLibelleSourceLabels.ts`**. **Libellé de ligne = membre d’équipe** : **`sourceLibelleLignes === "MEMBRE_EQUIPE"`** proposé à la création / édition du planning (`planningLibelleLignesSourceOptions` = toutes les valeurs de l’enum) ; modale ligne : liste **directeur + équipe** (`membresDirecteurEtEquipePourModal` / `optionsPourSource`), payload **`libelleUtilisateurTokenId`**. **Exception React Compiler** : ce fichier conserve **`useMemo`** (lignes triées, blocs, grilles triées, etc.) ; gros écran, interactions DnD et table.

- **Horaires du séjour** — **`sejour-horaire.service.ts`** (`sejourHoraireService` : `getHorairesDuSejour`, `getHoraireById`, `creerHoraire`, `modifierHoraire`, `supprimerHoraire`) — préfixe `/sejours/{sejourId}/horaires`. **`ListeHoraires.tsx`** + **`ListeHoraires.module.scss`** : CRUD modals, validation client via **`validerLibelleHoraire`** / **`LIBELLE_HORAIRE_REGEX`** (`helpers/validerLibelleHoraire.ts`). **Affichage et état** : **`trierHorairesChronologiquement`** (`helpers/trierHorairesChronologiquement.ts`) — tri par heure réelle du libellé, puis `id`. **`detailsSejourLoader`** : **`getHorairesDuSejour`** dans le `Promise.all`. **Page Paramétrage** (`DetailsSejourParametrage`) : panneau accordéon dédié « Horaires » ; état local + **`useRevalidator`** après mutations ; **vue générale** : données horaires pour **`ListePlanningsOrganisation`** via le loader. Types **`HoraireDto`**, **`SaveHoraireRequest`** dans `api.d.ts`.

- **Header séjour (DIRECTION + BASIC_USER)** — `Header.tsx` : pour **`DIRECTION`** ou **`BASIC_USER`** (**`isParticipantSejour`**), lien **Mes séjours** **`/mes-sejours`** ; en contexte séjour, **fil** + segments **Vue générale** / **Organisation** / **Activités** / **Menus** / **Sanitaire** / **Chambres** ; **Paramétrage** si **`peutGererMembresEquipeSejour`**. **Responsive** : **`Navbar expand="xxl"`** (burger **&lt; 1400px**, variable SCSS **`$header-nav-burger-max`**) ; **`useLayoutEffect`** + **`observeSiteHeaderHeight`** (`helpers/siteHeaderHeight.ts`) met à jour **`--site-header-height`** (header multi-lignes, menu burger). Styles **`Header.module.scss`** (`directorNav`, …).

- **Types d’activité — revalidation de route** — `ListeTypesActivite.tsx` : après **création / édition / suppression** réussies, `useRevalidator().revalidate()` pour recharger le loader **`sejour-detail`** (cohérence `typesActivite` + **activités** sur l’onglet Activités).

- **Liste des activités — responsive & calendrier** — **Vue liste** (`ListeActivitesListe` + SCSS) : sous **max-width 991.98px**, barre **Afficher / Masquer les filtres** + pastille **Filtres actifs** ; le panneau de filtres est replié par défaut sous **768px** (`data-liste-filtres-ouverts`). **Barre supérieure** (boutons + filtres) **fixe** sous le header du site (`.topPinnedStack`, variables `--liste-act-site-header`, `--liste-act-top-pinned-height`, `--liste-act-pinned-left`, `--liste-act-pinned-width` (mesure JS), `.topPinnedStackSpacer`). **Vue calendrier** : fenêtre **1 / 3 / 7 jours** et navigation de période pilotées par le hook **`useCalendrierFenetreJours`** (réutilise **`listeActivitesUtils`** : **`bornesDebutFenetreCalendrier`**, etc.) ; **`CalendrierNavigationPeriode`** ; **`CalendrierFiltresPlanning`** (dropdowns multi-sélection animateurs & groupes, **Réinitialiser** les filtres).

- **Détail séjour — sous-routes** — Route `/mes-sejours/:id` avec **`id: "sejour-detail"`** et **`detailsSejourLoader`** ; **`SejourDetailOutlet`** ; index → **`DetailsSejourOverview`**, **`organisation`** → **`DetailsSejourOrganisationLayout`** / **`DetailsSejourOrganisation`**, **`activites`** → **`DetailsSejourActivites`**, **`menus`** → **`DetailsSejourMenus`**, **`parametrage`** → **`DetailsSejourParametrage`** (réservé **directeur / adjoint**, sinon redirection). **`useRouteLoaderData("sejour-detail")`**. **Header** : fil séjour ; segments **Vue générale** / **Organisation** / **Activités** / **Menus** ; **Paramétrage** si gestionnaire. **`ProtectedRoute`** : **`[RoleSysteme.DIRECTION, RoleSysteme.BASIC_USER]`**.

- **Accordéons vue générale** — Ids **`5`**, **`2`**, **`3`**, **`4`** (**Réunions**, **Équipe**, **Liste des enfants**, **Groupes**) ; **informations générales** (description, lieu, dates, durée) en bandeau **`.overviewInfos`** au-dessus des accordéons (plus d’accordéon **`1`**) ; ordre par défaut **`OVERVIEW_ACCORDION_IDS`** : `["5","2","3","4"]`. **État initial** : **aucun** accordéon ouvert (`[]`) ; ouverture via **`location.state.openAccordion`** (retour dossier enfant, etc.). **Plannings organisation** : page **`/mes-sejours/:id/organisation`**. **Paramétrage** : page **`DetailsSejourParametrage`**, accordéons **`1`–`6`**, clé `enjoy.detailsSejour.parametrageAccordionOrder.{sejourId}`. Réordonnancement vue générale : clé `enjoy.detailsSejour.overviewAccordionOrder.{sejourId}` ; migration depuis l’ancienne `enjoy.detailsSejour.accordionOrder.{sejourId}`.

- **Moments réordonnables** — **`trierMomentsChronologiquement`**, **`sejourMomentService.reordonnerMoments`** (`PUT …/moments/reorder`, **`ReorderMomentsRequest`**), UI Monter/Descendre dans **`ListeMoments`** ; sync état parent **`DetailsSejourParametrage`**.

- **Types d’activité (par séjour)** — **`sejour-type-activite.service.ts`** (`getTypesActiviteDuSejour`, `getTypeActiviteById`, `creerTypeActivite`, `modifierTypeActivite`, `supprimerTypeActivite`) — préfixe `/sejours/{sejourId}/types-activite`. **`ListeTypesActivite.tsx`** : deux **accordéons internes** (Collapse reactstrap) — **types par défaut** (`predefini`, cartes étroites centrées, pas de modifier/supprimer) vs **types propres au séjour** (CRUD modals) ; après chaque **mutation** réussie, **`revalidate()`** sur la route (loader **`sejour-detail`**, alignement avec l’onglet **Activités**). Échec **DELETE** si le type est encore utilisé par des activités : message rouge fixe *« Impossible de supprimer ce type car des activités l'utilisent encore. »*, bouton **Confirmer la suppression** masqué, seul **Fermer** reste (détection via texte d’erreur API `IllegalArgumentException` côté **`TypeActiviteServiceImpl`**). **`DetailsSejourParametrage`** : liste des types sur la page Paramétrage ; callbacks pour garder les **`ActiviteDto`** alignés en mémoire locale où pertinent. Types **`TypeActiviteDto`**, **`SaveTypeActiviteRequest`**, **`ActiviteDto.typeActivite`**, **`typeActiviteId`** obligatoire sur **`CreateActiviteRequest`** / **`UpdateActiviteRequest`** dans `api.d.ts`.

- **Moments du séjour (créneaux)** — **`sejour-moment.service.ts`** (`sejourMomentService` : `getMomentsDuSejour`, `getMomentById`, `creerMoment`, `modifierMoment`, `supprimerMoment`, **`reordonnerMoments`**) — préfixe `/sejours/{sejourId}/moments`. **`ListeMoments.tsx`** : ordre **`trierMomentsChronologiquement`**, CRUD via modals, réordonnancement API (voir *Moments réordonnables* ci‑dessus). **`detailsSejourLoader`** inclut **`getMomentsDuSejour`**. Panneau Moments sur **`DetailsSejourParametrage`**. **`ListeActivites`** (page **`DetailsSejourActivites`**) : prop **`moments`** ; **`momentId`** obligatoire si des moments existent ; création bloquée si **`moments.length === 0`**. Types **`MomentDto`** (**`ordre`**), **`SaveMomentRequest`**, **`ReorderMomentsRequest`**, **`ActiviteDto.moment`**, **`momentId`** dans `api.d.ts`.

- **Lieux & activités (alignement API)** — `api.d.ts` : **`LieuDto`** / **`SaveLieuRequest`** avec **`partageableEntreAnimateurs`**, **`nombreMaxActivitesSimultanees`** ; **`ActiviteDto`** avec **`lieu`**, **`avertissementLieu`** ; **`CreateActiviteRequest` / `UpdateActiviteRequest`** avec **`lieuId`** optionnel et **`typeActiviteId`** obligatoire. **`ListeLieux.tsx`** : case à cocher **partage entre animateurs**, champ **nombre max d’activités le même jour** (≥ 2 si partage), résumé partage sur chaque carte, payload **`SaveLieuRequest`** complet. **Module liste des activités** (**`DetailsSejourActivites`**) : orchestration dans **`ListeActivites.tsx`** (modal CRUD, état, appels **`sejourActiviteService`**, bascule **liste / calendrier**) ; affichage délégué à **`ListeActivitesListe.tsx`** (filtres + tableau) et **`ListeActivitesCalendrier.tsx`** (planning, navigation période, filtres animateurs/groupes) ; types partagés **`listeActivitesTypes.ts`**, utilitaires dates/tri/regroupements **`listeActivitesUtils.ts`**, styles **`ListeActivites.module.scss`**. Props : **`lieux`**, **`typesActivite`**, **`moments`**, etc. Sélecteur **type d’activité** (optgroups *par défaut* / *du séjour*), validation obligatoire ; création désactivée si **`typesActivite.length === 0`** ; **métadonnées** (moment, lieu, groupes, animateurs, type) en **`metaGrid`** flex **wrap**. Sélection **lieu optionnelle** avec **filtre par emplacement** (Tous / Intérieur / Extérieur / Hors centre — `EmplacementLieuValues` / `EmplacementLieuLabels`), texte d’aide sur le **partage** du lieu choisi, cartes affichant **lieu + règle de partage** ; en édition, **`lieuId: null`** si « Aucun lieu » ; après POST/PUT, concaténation de **`avertissementLieu`** au message de succès (modal avec classe **`successBody`** / `pre-wrap`). Styles : **`lieuHint`**, **`successBody`** dans `ListeActivites.module.scss`. **Exception doc React Compiler** : **`ListeActivites.tsx`** conserve encore **`useMemo` / `useCallback` / effets** au-delà du seul filtre lieu (dérivés calendrier, filtres liste, handlers stables vers les sous-composants) — voir [decisions-architecturales.md](decisions-architecturales.md).

- **Vue générale & Paramétrage** — **`DetailsSejourOverview`** : bandeau **`.overviewInfos`** (description, lieu, dates, durée) ; accordéons Réunions, équipe, enfants, groupes (**fermés** par défaut). **`SectionReunionsSejour`** reçoit **`initialReunions`** depuis le loader (pas de skeleton au montage). **`DetailsSejourOrganisation`** : **`ListePlanningsOrganisation`** (liste / éditeur selon route). **`DetailsSejourParametrage`** : **`ListeLieux`**, **`ListeMoments`**, **`ListeHoraires`**, **`ListeTypesActivite`**, **`ListeReferencesAlimentaires`**, **`ParametrageAffichageMenus`** (les **activités** restent sur **`/activites`**). Données chargées dans **`detailsSejourLoader`** : `Promise.all` — séjour, enfants, groupes, **lieux**, **moments**, **horaires**, activités, **types d’activité**, **`reunions`** (`sejourLieuService`, **`sejourMomentService`**, **`sejourHoraireService`**, `sejourActiviteService`, **`sejourTypeActiviteService`**, **`sejourReunionService.listerReunions`**) + en parallèle **`planningGrilles`** via **`listerGrilles`**. API lieux : **`sejour-lieu.service.ts`** ; API moments : **`sejour-moment.service.ts`** ; API horaires : **`sejour-horaire.service.ts`** ; réunions : **`sejour-reunion.service.ts`** ; plannings grilles : **`sejour-planning-grille.service.ts`** ; types **`LieuDto`** / **`SaveLieuRequest`**, **`MomentDto`** / **`SaveMomentRequest`**, **`HoraireDto`** / **`SaveHoraireRequest`**, **`ReunionDto`** / **`SaveReunionRequest`**, **`EmplacementLieu`**, DTOs planning (`PlanningGrille*`, `PlanningLigne*`, etc.) dans `api.d.ts` ; libellés **`EmplacementLieuLabels`**. Le **lieu principal** du séjour (`lieuDuSejour`) reste dans **Informations générales**. Retour depuis le dossier enfant : **`/mes-sejours/:id`** avec **`openAccordion: '3'`** ou **`'4'`** (+ `expandedGroupeId` si besoin).

- **Référents de groupe** (membres de l'équipe désignés pour un groupe) :
  - Composant `ReferentsSelector.tsx` : champ personnalisé pour le `Form` générique ; multi-sélection par cases à cocher sur l'équipe du séjour (`EquipePerson` : `tokenId`, `nom`, `prenom`). La valeur du champ est une **chaîne JSON** `JSON.stringify(string[])` des `tokenId` sélectionnés (parse côté soumission).
  - Si l'équipe est vide : message invitant à ajouter des membres avant de désigner des référents.
  - `CreateGroupeForm.tsx` : champ `referents` + après succès création/édition, **synchronisation par diff** avec l'API (`ajouterReferent` / `retirerReferent`) par rapport aux référents actuels du `GroupeDto`.
  - `ListeGroupes.tsx` : affichage des référents dans l'accordéon (« Référents : » + noms joint par virgule).
  - Types : `ReferentInfos`, `GroupeDto.referents`, `AjouterReferentRequest.referentTokenId`.

- **Notice d'import Excel** (composant `ImportExcelEnfants`) :
  - Icône d'information (faCircleInfo) à côté du bouton « Importer depuis Excel » ouvre une modal avec la notice des colonnes
  - Données chargées dynamiquement via `GET /api/v1/sejours/{sejourId}/enfants/import/spec` (`getExcelImportSpec` dans `sejour-enfant.service.ts`)
  - **Logique groupes (ET/OU)** : l'en-tête doit contenir **tous** les groupes (ET), avec **au moins un** mot par groupe (OU). Ex. : Email parent 1 = (email ou mail) ET parent ET 1. Synchronisé avec le backend `ExcelImportSpec` (groupesMotsCles).
  - Affiche colonnes obligatoires, optionnelles avec mots-clés formatés (join " ET " entre groupes)
  - Précise que les majuscules et les espaces sont autorisés dans les en-têtes Excel (détection flexible côté backend)
  - `formatKeywordForDisplay()` : formate les mots-clés pour l'affichage (espaces) ; ne modifie pas les chaînes déjà formatées par l'API (ex. "(email ou mail)")
  - Types : `ExcelImportSpecResponse`, `ExcelImportColumnSpec` dans `api.d.ts`

- **Refactoring des services séjour** (séparation des responsabilités) :
  - `sejour.service.ts` : CRUD des séjours uniquement (getAllSejours, getSejourById, **`getAllSejoursByUtilisateur`** — anciennement `getAllSejoursByDirecteur`, endpoint `/sejours/utilisateur/{utilisateurTokenId}`, addSejour, updateSejour, deleteSejour)
  - `sejour-equipe.service.ts` : Gestion de l'équipe (ajouterNouveauMembreEquipe, ajouterMembreExistantEquipe, modifierRoleMembreEquipe, supprimerMembreEquipe)
  - `sejour-enfant.service.ts` : Gestion des enfants (getEnfantsDuSejour, getDossierEnfant, updateDossierEnfant, supprimerEnfantDuSejour, supprimerTousLesEnfants, creerEtAjouterEnfant, modifierEnfant, importerEnfantsExcel)
  - `sejour-groupe.service.ts` : Gestion des groupes (remplace groupe.service.ts, même API avec export sejourGroupeService)
  - `sejour-activite.service.ts` : Activités d’un séjour (`getActivitesDuSejour`, `getActiviteById`, `creerActivite`, `modifierActivite`, `supprimerActivite`) — préfixe `/sejours/{sejourId}/activites`
  - `sejour-moment.service.ts` : Moments d’un séjour (`getMomentsDuSejour`, `getMomentById`, `creerMoment`, `modifierMoment`, `supprimerMoment`, **`reordonnerMoments`**) — préfixe `/sejours/{sejourId}/moments` ; export `sejourMomentService`
  - `sejour-type-activite.service.ts` : Types d’activité d’un séjour (`getTypesActiviteDuSejour`, `getTypeActiviteById`, `creerTypeActivite`, `modifierTypeActivite`, `supprimerTypeActivite`) — préfixe `/sejours/{sejourId}/types-activite` ; export `sejourTypeActiviteService`
  - `sejour-lieu.service.ts` : Lieux d’un séjour (`getLieuxDuSejour`, `getLieuById`, `creerLieu`, `modifierLieu`, `supprimerLieu`) — préfixe `/sejours/{sejourId}/lieux` ; export `sejourLieuService`
  - `sejour-horaire.service.ts` : Horaires d’un séjour (`getHorairesDuSejour`, `getHoraireById`, `creerHoraire`, `modifierHoraire`, `supprimerHoraire`) — préfixe `/sejours/{sejourId}/horaires` ; export `sejourHoraireService`
  - **`sejour-reunion.service.ts`** : Comptes rendus de réunion (`listerReunions`, `getReunion`, `creerReunion`, `modifierReunion`, `supprimerReunion`) — préfixe `/sejours/{sejourId}/reunions` ; export **`sejourReunionService`** ; **`listerReunions`** chargée par **`detailsSejourLoader`**

- **Helper centralisé pour la gestion des erreurs API** (`helpers/axiosError.ts` + **`helpers/routeError.ts`**) :
  - `validateResponseStatus(response, expectedStatus)` : Vérifie le code HTTP attendu
  - **`NETWORK_ERROR_MESSAGE`**, **`isNetworkError`**, **`getUserFacingErrorMessage`** : erreurs réseau (pas de **`response`**) avec message FR
  - **`getApiErrorMessage(errorData, whenUnknown)`** : construit le message affiché à partir du corps JSON (**`message`** métier prioritaire, puis **`error`**, puis agrégation validation)
  - `adaptAxiosError(error, options)` : Adapte les erreurs Axios (message, format validation 400, preserveResponseData) ; réseau → **`NETWORK_ERROR_MESSAGE`**
  - **`routeError.ts`** : **`throwRouteLoaderError`**, **`navigateToRouteError`**, **`ErreurAffichage`**, routes **`/erreur`** / boundary / 404 catch-all
    - Utilisé dans les services : séjour, équipe, enfant, groupe, **activités**, utilisateur ; **`getApiErrorMessage`** aussi dans **`DossierEnfant`**, **`CreateGroupeForm`**, **`ImportExcelEnfants`**, **`Profil.tsx`**, **`UserForm.tsx`**

- **Gestion des groupes** (section Groupes dans **`DetailsSejourOverview`**) :
  - `ListeGroupes.tsx` : liste des groupes avec accordéon, création/édition/suppression, ajout/retrait manuel d'enfants
  - `CreateGroupeForm.tsx` : formulaire création/édition avec types THEMATIQUE, AGE, NIVEAU_SCOLAIRE
  - Bouton « Ajouter les X enfant(s) de la tranche » (vert, aligné à droite) pour groupes AGE/NIVEAU_SCOLAIRE — ajoute en masse les enfants correspondant à la tranche sans doublons
  - Fallback frontend : si le backend renvoie un groupe vide à la création (ex. groupe créé avant la liste d'enfants), le frontend ajoute automatiquement les enfants correspondant à la tranche via `ajouterEnfantAuGroupe`
  - Helper `getEnfantsMatchingTranche()` dans `helpers/groupeTranche.ts` — filtre les enfants par tranche (âge ou niveau scolaire), utilisé par ListeGroupes et CreateGroupeForm
  - `calculerAge(dateNaissance, referenceDate?)` : le paramètre optionnel `referenceDate` permet de calculer l'âge à une date donnée (ex. date de début du séjour pour les groupes par âge)

- **Implémentation complète du dossier enfant** (services dans `sejour-enfant.service.ts`) :
  - Page `DossierEnfant` (`Pages/_Sejours/DossierEnfant/`) : affichage et modification du dossier (contacts parents, médical, traitements)
  - Bouton "Modifier" ouvre un modal avec `DossierEnfantForm` — PUT complet via `updateDossierEnfant(sejourId, enfantId, request)`
  - Route `/mes-sejours/:sejourId/enfants/:enfantId/dossier` avec loader `dossierEnfantLoader`
  - Icône dossier dans `ListeEnfants` (props `canDossier`/`onDossier` du composant `Liste`) — navigation vers la page dossier au clic
  - Bouton Retour : revient vers **`/mes-sejours/:sejourId`** (vue générale) en conservant le contexte. Depuis ListeEnfants : `state: { from: 'enfants', openAccordion: '3' }`. Depuis ListeGroupes : `state: { from: 'groupes', openAccordion: '4', expandedGroupeId }` — ouvre l'accordéon Groupes et scroll vers le groupe concerné
  - Scroll automatique en haut de page à l'arrivée sur le dossier (`useEffect` + `window.scrollTo`)
  - Styles : `.infoStack` pour espacement 1rem entre blocs d'infos, `.enfantNameBadge` avec police sans-serif pour lisibilité du nom

- Migration complète vers React 19 avec `babel-plugin-react-compiler`
- Refactoring des composants de liste (`ListeSejoursAdmin`, **`MesSejours`** — anciennement `ListeSejoursDirecteur`)
- Optimisation des formulaires avec suppression des `useMemo`/`useCallback` inutiles
- Détail séjour : **`DetailsSejourOverview`** + **`DetailsSejourActivites`** + **`DetailsSejourParametrage`**, **`SejourDetailOutlet`**, loader route **`sejour-detail`**
- **Implémentation complète de la gestion des enfants** :
  - Composant `AddEnfantForm` pour créer/modifier un enfant (utilise le Form générique)
  - Composant `ListeEnfants` pour afficher et gérer la liste des enfants d'un séjour (CRUD complet)
  - Composant `ImportExcelEnfants` pour importer des enfants depuis un fichier Excel
  - Intégration dans **`DetailsSejourOverview`** (accordéon dédié)
  - Services complets dans `sejour-enfant.service.ts` pour tous les endpoints enfants
  - Utilisation des types centralisés de `api.d.ts` (`EnfantDto`, `CreateEnfantRequest`, `ExcelImportResponse`)
- **Améliorations récentes** :
  - Tri alphabétique automatique des enfants (par nom puis prénom) dans `getEnfantsDuSejour()`
  - Formatage du niveau scolaire dans `ListeEnfants` avec `NiveauScolaireLabels` (affichage "5ème" au lieu de "CINQUIEME")
  - Nettoyage du code : suppression de la méthode non utilisée `ajouterEnfantAuSejour` dans `sejour.service.ts`
  - Mise à jour de `baseline-browser-mapping` vers 2.9.19 (dernière version) pour supprimer l'avertissement "data over two months old"
  - Correction du type dans `MesSejours.tsx` (anciennement `ListeSejoursDirecteur.tsx`) : utiliser `SejourDTO` (et non `Sejour`) pour le paramètre de `handleView` et les données de séjour

## En cours

Affinements **planning activités / calendrier** (filtres, UX, perf) — **`ListeActivitesCalendrier.tsx`**. Les **plannings direction** (grilles par séjour) sont gérés dans **`ListePlanningsOrganisation`** ; des évolutions plus larges (affectations avancées, vues suppl.) restent sur la [roadmap](roadmap.md).
